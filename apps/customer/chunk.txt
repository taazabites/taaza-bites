
            {/* Travel Persona & Pattern Insights Badge */}
            <div className="mb-6 p-4 rounded-2xl bg-gradient-to-r from-emerald-500/10 via-teal-500/10 to-indigo-500/10 border border-emerald-500/20 dark:border-emerald-500/30 flex items-center justify-between gap-4">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 rounded-xl bg-emerald-500 text-white flex items-center justify-center shrink-0 shadow-md shadow-emerald-500/20">
                  {profile.iconName === 'Sun' ? <Sun className="w-5 h-5" /> : profile.iconName === 'Compass' ? <Compass className="w-5 h-5" /> : <Plane className="w-5 h-5" />}
                </div>
                <div>
                  <div className="flex items-center gap-2">
                    <p className="text-xs font-black text-zinc-900 dark:text-white uppercase tracking-wider">
                      {profile.personaTitle}
                    </p>
                    <span className="text-[9px] font-bold px-1.5 py-0.2 rounded bg-emerald-500/20 text-emerald-700 dark:text-emerald-300">
                      Avg {profile.avgPauseDurationDays} days
                    </span>
                  </div>
                  <p className="text-[11px] text-zinc-500 dark:text-zinc-400 font-medium line-clamp-1">
                    {profile.mealsPreserved} meals preserved across {profile.totalPastPauses} vacations (~₹{profile.amountSaved.toLocaleString()} saved)
                  </p>
                </div>
              </div>
              <div className="hidden sm:block text-right shrink-0">
                <span className="text-[10px] font-black uppercase text-emerald-600 dark:text-emerald-400 bg-white/80 dark:bg-zinc-900/80 px-2.5 py-1 rounded-lg border border-emerald-500/20 shadow-2xs">
                  Smart Resume Active
                </span>
              </div>
            </div>

            {/* Mode Switcher Tabs */}
            <div className="flex p-1 bg-zinc-100 dark:bg-zinc-900 rounded-2xl mb-6 gap-1 border border-zinc-200/50 dark:border-zinc-800">
              <button
                onClick={() => { triggerHaptic('light'); setActiveTab('smart'); }}
                className={cn(
                  "flex-1 py-2.5 px-3 rounded-xl text-xs font-black uppercase tracking-wider transition-all flex items-center justify-center gap-1.5 cursor-pointer",
                  activeTab === 'smart'
                    ? "bg-white dark:bg-zinc-800 text-zinc-950 dark:text-white shadow-xs"
                    : "text-zinc-500 hover:text-zinc-900 dark:hover:text-white"
                )}
              >
                <Sparkles className="w-3.5 h-3.5 text-emerald-500" />
                Smart AI Resume
              </button>
              <button
                onClick={() => { triggerHaptic('light'); setActiveTab('custom'); }}
                className={cn(
                  "flex-1 py-2.5 px-3 rounded-xl text-xs font-black uppercase tracking-wider transition-all flex items-center justify-center gap-1.5 cursor-pointer",
                  activeTab === 'custom'
                    ? "bg-white dark:bg-zinc-800 text-zinc-950 dark:text-white shadow-xs"
                    : "text-zinc-500 hover:text-zinc-900 dark:hover:text-white"
                )}
              >
                <Calendar className="w-3.5 h-3.5" />
                Custom Dates
              </button>
              <button
                onClick={() => { triggerHaptic('light'); setActiveTab('holidays'); }}
                className={cn(
                  "flex-1 py-2.5 px-3 rounded-xl text-xs font-black uppercase tracking-wider transition-all flex items-center justify-center gap-1.5 cursor-pointer",
                  activeTab === 'holidays'
                    ? "bg-white dark:bg-zinc-800 text-zinc-950 dark:text-white shadow-xs"
                    : "text-zinc-500 hover:text-zinc-900 dark:hover:text-white"
                )}
              >
                <Plane className="w-3.5 h-3.5 text-indigo-500" />
                Holidays
              </button>
            </div>

            {/* TAB 1: SMART AI RESUME SUGGESTIONS */}
            {activeTab === 'smart' && (
              <div className="space-y-4 mb-6">
                <div className="flex items-center justify-between">
                  <label className="text-xs font-black uppercase text-zinc-500 dark:text-zinc-400 tracking-wider">
                    Departure / Start Date
                  </label>
                  <span className="text-[10px] font-bold text-emerald-600 dark:text-emerald-400 flex items-center gap-1">
                    <Info className="w-3 h-3" /> Select start date to recalculate suggestions
                  </span>
                </div>
                <input 
                  type="date" 
                  min={todayStr}
                  value={startDate}
                  onChange={(e) => handleStartDateChange(e.target.value)}
                  className="w-full p-3.5 rounded-2xl border border-zinc-200 dark:border-white/10 bg-zinc-50 dark:bg-zinc-900/60 font-bold text-sm"
                />

                <div className="pt-2">
                  <label className="text-xs font-black uppercase text-zinc-500 dark:text-zinc-400 tracking-wider block mb-2.5">
                    Recommended Optimal Resume Dates
                  </label>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                    {smartSuggestions.map((s) => {
                      const isSelected = selectedSuggestionId === s.id;
                      return (
                        <div
                          key={s.id}
                          onClick={() => handleSelectSuggestion(s)}
                          className={cn(
                            "p-4 rounded-2xl border transition-all cursor-pointer relative flex flex-col justify-between group",
                            isSelected 
                              ? "border-emerald-500 bg-emerald-50/60 dark:bg-emerald-500/10 shadow-md ring-2 ring-emerald-500/20" 
                              : "border-zinc-200 dark:border-white/10 bg-zinc-50/50 dark:bg-zinc-900/40 hover:border-zinc-300 dark:hover:border-white/20"
                          )}
                        >
                          <div className="flex justify-between items-start mb-2">
                            <span className="text-[9px] font-black uppercase px-2 py-0.5 rounded-md bg-emerald-500/15 text-emerald-700 dark:text-emerald-300 border border-emerald-500/20">
                              {s.badgeText}
                            </span>
                            {isSelected && (
                              <CheckCircle2 className="w-4 h-4 text-emerald-600 dark:text-emerald-400 shrink-0" />
                            )}
                          </div>
                          <div>
                            <h4 className="text-xs font-black text-zinc-900 dark:text-white mb-0.5">
                              {s.title}
                            </h4>
                            <p className="text-[11px] font-bold text-emerald-700 dark:text-emerald-400 mb-1 flex items-center gap-1">
                              Resume: {format(parseISO(s.resumeDate), "EEE, MMM d, yyyy")}
                            </p>
                            <p className="text-[10px] text-zinc-500 dark:text-zinc-400 leading-tight mb-2">
                              {s.reasoning}
                            </p>
